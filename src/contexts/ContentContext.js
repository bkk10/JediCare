Rbsthbhdin
oTdaSavdaaToSav✅ ❌ fil:dn'tcah he ppsais✅ ❌  Run only once onoun

 // Dbouce sav to lolStoge
  useEffect(()>{
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.curent);
    }
    
    timeoutRef.crret = setTimeout(() =>{
      saveTStorage(cotent);
    }, 1000); //Debu for 1secd
 }, [cent]);